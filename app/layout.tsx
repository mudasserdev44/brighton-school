import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import Header from "@/components/Header";
import Footer from "@/components/Footer";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "BSL- Brighton School of Lahore",
  description: "Discover exceptional education at BSL - Brighton School of Lahore. Our commitment to academic excellence, innovative teaching methods, and a nurturing environment empowers students to reach their full potential. Join our vibrant community and embark on a journey of learning, growth, and future success.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">
        <Header />
        <main>
        {children}
        </main>
        <Footer />
        </body>
    </html>
  );
}
